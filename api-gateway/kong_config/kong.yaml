_format_version: "1.1"

services:
- name: my-service
  url: http://my-service.example.com
  routes:
  - name: my-route
    paths:
    - /my-service

- name: another-service
  url: http://another-service.example.com:8080
  routes:
  - name: another-route
    paths:
    - /another-service